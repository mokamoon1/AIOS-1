# Alembic migration versions directory (ADR-0006).
# Each migration is versioned, reversible, and stored under version control.
# This directory is empty until the initial schema migration is created.
